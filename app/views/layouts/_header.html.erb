<div class="row">
  <div class="col-md-4">
    <h1><%=link_to Forum.first.name, root_path%></h1>
  </div>
  <div class="col-md-4 col-md-offset-4">
    <%= link_to 'Sign In', signin_path if current_user.nil? %>
    <%= link_to 'Sign Out', signout_path, method: :delete unless current_user.nil? %>
  </div>
</div>
<hr/>